name: Run Scraper

on:
  schedule:
    - cron: "0 12 * * *"   # 7 AM Perú = 12:00 UTC
  workflow_dispatch: {}
  
jobs:
  run:
    runs-on: ubuntu-latest

    env:
      GOOGLE_TOKEN: ${{ secrets.GOOGLE_TOKEN }}

    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.10"

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Run scraper
        run: python main.py
